
import React from 'react';

const Calendar = ({ title, content }) => {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>{content}</p>
      <p>{content}</p>
      <p>{content}</p>
      <p>{content}</p>
      <p>{content}</p>
      <p>{content}</p>
      <p>{content}</p>
    </div>
  );
};

export default Calendar;
